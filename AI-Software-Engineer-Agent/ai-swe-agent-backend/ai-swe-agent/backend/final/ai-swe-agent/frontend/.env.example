# Copy to .env.local and adjust if your backend isn't on the default port.
# Both the HTTP health check and the WebSocket connection derive from this
# single value (see src/lib/config.ts).
VITE_API_BASE_URL=http://localhost:8000
