# Copy to .env and adjust. Every value here has a sane default in
# app/config.py, so an empty .env (or none at all) is perfectly valid.

# --- Ollama ---------------------------------------------------------------
OLLAMA_BASE_URL=http://localhost:11434

# --- Model routing (each must already be `ollama pull`-ed) -----------------
PLANNER_MODEL=llama3:8b
CODER_MODEL=qwen2.5-coder:7b
REVIEWER_MODEL=codellama:13b-instruct

# --- Loop behaviour ----------------------------------------------------------
MAX_DEBUG_ITERATIONS=4
PLANNER_TEMPERATURE=0.2
CODER_TEMPERATURE=0.1
REVIEWER_TEMPERATURE=0.1

# --- Sandbox -------------------------------------------------------------------
EXECUTOR_BACKEND=docker
SANDBOX_IMAGE=agent-sandbox:latest
SANDBOX_TIMEOUT_SECONDS=20
SANDBOX_MEMORY_LIMIT=512m
SANDBOX_NANO_CPUS=1000000000
SANDBOX_NETWORK_DISABLED=True
WORKSPACE_FILENAME=solution.py

# --- CORS (comma-separated origins; the frontend's Vite dev server) ------------
ALLOWED_ORIGINS=http://localhost:5173
